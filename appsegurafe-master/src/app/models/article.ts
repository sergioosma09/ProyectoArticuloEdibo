export interface IArticle {
  id: string,
  author: string,
  title: string,
  journal: string,
  year: number,
  volume: number,
  number: number,
  articleNumber: number,
  url: string,
  issn: string,
  abstract: string,
  doi: string
}